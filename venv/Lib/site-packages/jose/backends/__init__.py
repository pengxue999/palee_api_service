from jose.backends.native import get_random_bytes

try:
    from jose.backends.cryptography_backend import CryptographyRSAKey as RSAKey
except ImportError:
    try:
        from jose.backends.rsa_backend import RSAKey
    except ImportError:
        RSAKey = None

try:
    from jose.backends.cryptography_backend import CryptographyECKey as ECKey
except ImportError:
    from jose.backends.ecdsa_backend import ECDSAECKey as ECKey

try:
    from jose.backends.cryptography_backend import CryptographyAESKey as AESKey
except ImportError:
    AESKey = None

try:
    from jose.backends.cryptography_backend import CryptographyHMACKey as HMACKey
except ImportError:
    from jose.backends.native import HMACKey

from .base import DIRKey
